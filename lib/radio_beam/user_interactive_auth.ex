defmodule RadioBeam.UserInteractiveAuth do
  @moduledoc """
  Main module exposing functionality to facilitate the [User-Interactive Authentication API](https://spec.matrix.org/latest/client-server-api/#user-interactive-authentication-api).

  will impl this later alongside RadioBeamWeb.Plugs.UserInteractiveAuth later when more
  than standard username/password registration/login support is added
  """
end
